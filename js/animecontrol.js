anime({
    targets: '.draw .st69',
    strokeDashoffset: [anime.setDashoffset, 0],
    easing: 'easeInOutQuad',
    duration: 4500,
    delay: function(el, i) { return i * 250 },
    direction: 'alternate',
    loop: true
  });
  anime({
    targets: '.draw .st40020',
    strokeDashoffset: [anime.setDashoffset, 0],
    easing: 'easeInOutQuad',
    duration: 100,
    delay: function(el, i) { return i * 350 },
    direction: 'alternate',
    loop: true
  });
  anime({
    targets: '.draw .st89',
    strokeDashoffset: [anime.setDashoffset, 0],
    easing: 'easeInOutQuad',
    duration: 5000,
    delay: function(el, i) { return i * 350 },
    direction: 'alternate',
    loop: true
  });
  anime({
    targets: '.draw .st90',
    strokeDashoffset: [anime.setDashoffset, 0],
    easing: 'easeInOutQuad',
    duration: 2000,
    delay: function(el, i) { return i * 350 },
    direction: 'alternate',
    loop: true
  });